# npm run dev(vite 개발 서버, 기본 포트 5173)에서만 적용된다.
# 이 모드에서는 백엔드(uvicorn)가 다른 포트(기본 8000)에서 따로 떠 있으므로
# API_BASE를 명시적으로 지정해야 한다. 프로덕션 빌드(webapp/dist, 데스크톱 앱)는
# server.py가 프론트엔드까지 같은 오리진에서 서빙하므로 이 값이 필요 없다
# (src/api/client.ts의 기본값이 상대 경로로 자동 전환됨).
VITE_API_BASE=http://127.0.0.1:8000
